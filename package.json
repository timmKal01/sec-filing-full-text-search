{
    "name": "sec-filing-full-text-search",
    "version": "0.1.0",
    "type": "module",
    "description": "Search a keyword across every SEC filing type at once via the official EDGAR full-text search API.",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "apify": "^3.2.6"
    },
    "scripts": {
        "start": "node src/main.js"
    }
}
