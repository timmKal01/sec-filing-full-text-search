{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of SEC Filing Full-Text Search",
    "description": "One record per matching filing across any SEC form type.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
